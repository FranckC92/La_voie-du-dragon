<header>
	<nav class="navbar navbar-expand-lg fixed-top navbar-dark bg-black">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('app_home')}}"><img src="/assets/img/logo.png" 
	alt="logo voie du dragon" title="Accueil" class="img-fluid" width="50">La voie du Dragon</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample07" aria-controls="navbarsExample07" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarsExample07">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link" href="{{ path('app_blogs')}}">Le Blog</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('products')}}">Les Arts Martiaux</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="dropdown07" 
          data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Réglement Intérieur (RGPD)</a>
          <div class="dropdown-menu" aria-labelledby="dropdown07">
            <a class="dropdown-item" href="{{ path('app_c_g_u')}}">CGU</a>
            <a class="dropdown-item" href="{{ path('app_politique')}}">Politique de Confidentialité</a>
			<a class="dropdown-item" href="{{path('app_contact')}}">Nous contacter</a>
          </div>
        </li>
      </ul>
     	<div class="navbar-item-custom">
        {# Conditions if else Twig #}
				{% if app.user %}
					{# won't be escaped #}
						<a href="{{ path('app_account')}}">Mon compte
						<small>
							({{app.user.firstname|upper|raw}})</small>>
					</a>
					|
					<a href="{{ path('app_logout')}}">Déconnexion</a>
				{% else %}
					<a href="{{ path('app_login')}}">Connexion</a>
					|
					<a href="{{ path('app_register')}}">Inscription</a>
				{% endif %}
				{# finde la conditions twig if else #}
        </div>
    </div>
  </div>
</nav>
</header>
